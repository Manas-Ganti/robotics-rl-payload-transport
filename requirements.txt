# =============================================================================
# requirements.txt
# =============================================================================
# NOT INCLUDED HERE (install separately -- see setup_notes.md):
#   * Isaac Sim      -- installed via Omniverse or the NGC container
#   * Isaac Lab      -- installed from source against a matching Isaac Sim
#   * torch          -- ships WITH Isaac Sim's bundled Python; installing a
#                       second torch into that interpreter reliably breaks it
#   * ROS 2 / Nav2   -- apt packages, only needed for nav2 ros2_bridge mode
#
# Install into ISAAC SIM'S python, not the system python:
#   ${ISAACSIM_PATH}/python.sh -m pip install -r requirements.txt
# =============================================================================

# ---- core (needed everywhere, including the local GPU-less machine) ----
numpy>=1.24,<2.0          # <2.0: Isaac Sim 4.x bundles a NumPy 1.x build
pyyaml>=6.0

# ---- experiment tracking ----
wandb>=0.16.0

# ---- RL (A100 only; rsl_rl expects torch to already be present) ----
rsl-rl-lib>=2.0.2         # VERIFY ON A100: Isaac Lab pins a specific version.
                          # Check isaaclab's own setup.py before overriding.

# ---- analysis / plotting (runs locally on results CSVs) ----
matplotlib>=3.7.0

# ---- tests (local, no Isaac required) ----
pytest>=7.4.0
pytest-cov>=4.1.0

# ---- optional: SAC comparison path (see training/ppo_config.py) ----
# skrl>=1.3.0             # uncomment only when wiring algo.name: sac
